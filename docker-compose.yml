services:
  app1:
    build: .
    container_name: flask_app_1
    ports:
      - "5001:5000"
    restart: always
    environment:
      - APP_INSTANCE=1

  app2:
    build: .
    container_name: flask_app_2
    ports:
      - "5002:5000"
    restart: always
    environment:
      - APP_INSTANCE=2
